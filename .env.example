# PostgreSQL credentials for Docker Compose
# Wired to: db service (POSTGRES_DB/USER/PASSWORD)
# Wired to: backend service (DATABASE_NAME/USER/PASSWORD)
POSTGRES_DB=cafe_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres